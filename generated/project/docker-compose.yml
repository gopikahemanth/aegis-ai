version: '3.8'

services:
  productivity-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aegis_productivity_dashboard
    ports:
      - "8080:80"
    restart: unless-stopped