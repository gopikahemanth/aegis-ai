import React from 'react';
export interface Artwork { id: string | number; title?: string; imageUrl?: string; price?: number; }
export const auth.routes: React.FC<any> = (props: any) => <div className="p-4" {...props}>{props?.children || 'auth.routes'}</div>;
export default auth.routes;
